#include <cmocka_init.h>

/* Order here is important as math will be done later to see if these tests
 * structs are contiguous
 */

TEST(Test0)
{
    assert_true(1);
}

TEST(Test1)
{
    assert_true(1);
}
